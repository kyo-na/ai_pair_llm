
# Attention Pipeline – FPGA Graduation Edition v2

This is the **corrected graduation package**.

Included:
- CPU Golden Reference (decode, online softmax)
- CUDA Scalar GPU Kernel (FlashAttention decode)
- CUDA WMMA-ready Kernel Skeleton
- Pipeline Backend Selection
- KV Cache + GQA/MQA
- FPGA FSM Mapping
- Numerical Test Harness

This ZIP fixes the missing CUDA directory in v1.
